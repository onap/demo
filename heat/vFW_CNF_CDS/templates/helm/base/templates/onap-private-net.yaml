apiVersion: k8s.plugin.opnfv.org/v1alpha1
kind: Network
metadata:
  name: {{ .Values.onapPrivateNetworkName }}
spec:
  cniType : ovn4nfv
  ipv4Subnets:
  - name: subnet1
    subnet: {{ .Values.onapPrivateNetCidr }}
    gateway: {{ .Values.onapPrivateNetGw }}
