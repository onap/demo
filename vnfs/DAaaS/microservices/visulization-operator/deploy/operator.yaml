apiVersion: apps/v1
kind: Deployment
metadata:
  name: visulization-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: visulization-operator
  template:
    metadata:
      labels:
        name: visulization-operator
    spec:
      serviceAccountName: visulization-operator
      containers:
        - name: visulization-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - visulization-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "visulization-operator"
